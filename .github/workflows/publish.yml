name: Publish

on:
  workflow_dispatch:
  push:
    tags:
      - "*"

jobs:

  build:
    name: Build release APKs
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: set up JDK 17
        uses: actions/setup-java@v6
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      # The secret holds the keystore base64 encoded, since a secret carries text and not a binary
      # file. The signing config picks it up from the repository root by default.
      - name: Create release keystore
        env:
          RELEASE_KEYSTORE_BASE64: ${{ secrets.RELEASE_KEYSTORE_BASE64 }}
        run: |
          echo "$RELEASE_KEYSTORE_BASE64" | base64 -d > release.jks
          if [ ! -s release.jks ]; then
            echo "::error::RELEASE_KEYSTORE_BASE64 is empty or not valid base64"
            exit 1
          fi

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # Use dynamic flavor for 'free' version to reduce the download size. For the Play version, bundle
      # all of the cores instead since we can't download from Google Play.
      - name: Assemble release APKs
        env:
          RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_STORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: ./gradlew assembleFreeDynamicRelease assemblePlayBundleRelease

      - name: Upload free APK
        uses: actions/upload-artifact@v7
        with:
          name: apk-free-dynamic
          path: lemuroid-app/build/outputs/apk/freeDynamic/release/lemuroid-app-free-dynamic-release.apk
          if-no-files-found: error

      - name: Upload play APK
        uses: actions/upload-artifact@v7
        with:
          name: apk-play-bundle
          path: lemuroid-app/build/outputs/apk/playBundle/release/lemuroid-app-play-bundle-release.apk
          if-no-files-found: error

  publish_apk:
    name: Publish APKs on Github
    needs: [ build ]
    runs-on: ubuntu-latest
    # A manual run is only ever asked for the artifacts above. Releases are cut from tags.
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Create pre-release
        uses: softprops/action-gh-release@v3
        with:
          prerelease: true
          name: "Pre-release"
          files: |
            artifacts/lemuroid-app-free-dynamic-release.apk
            artifacts/lemuroid-app-play-bundle-release.apk
